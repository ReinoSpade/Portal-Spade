services:
  - type: web
    name: portal-spade
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_KEY
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: NODE_ENV
        value: production
